name: HTML to PNG

on:
  push:
    paths:
      - 'input/**'
  workflow_dispatch:

jobs:
  convert:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: main

      # ✅ تأكد إنه شايف آخر نسخة من main
      - name: Pull Latest Changes
        run: |
          git pull origin main
          echo "📂 Input folder contents:"
          ls -la input/ || echo "⚠️ input/ is empty"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Prepare Workspace
        run: mkdir -p input output temp

      - name: Create .gitignore
        run: |
          cat >> .gitignore << 'EOF'
          node_modules/
          package-lock.json
          package.json
          temp/
          EOF

      - name: Install Dependencies
        run: npm install puppeteer adm-zip

      - name: Run Conversion Script
        run: node convert.js

      - name: Check Generated Files
        run: |
          echo "📂 Output directory contents:"
          ls -R output/ || echo "⚠️ Output is empty!"
          if [ -z "$(ls -A output/ 2>/dev/null)" ]; then
            echo "❌ No PNG files generated — check convert.js logs"
            exit 1
          fi

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add output/ --force
          git rm input/*.zip --ignore-unmatch || true
          
          if [ -n "$(git diff --cached --name-only)" ]; then
            git commit -m "✅ Carousel Generated: $(date +'%Y-%m-%d %H:%M')"
            git pull --rebase origin main
            git push origin main || (git pull --rebase origin main && git push origin main)
            echo "✅ Push successful!"
          else
            echo "ℹ️ No new files in output — skipping commit."
          fi

      - name: Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-output
          path: output/
          retention-days: 1
